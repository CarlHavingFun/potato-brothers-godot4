# Retired pre-rebuild tests

These tests target the legacy `core/` runtime removed by the architecture
rebuild in commit `730520a`. They are retained for historical reference but
excluded from Godot resource scanning because their production types no longer
exist in the current `game/` runtime.

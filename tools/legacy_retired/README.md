# Retired pre-rebuild tools

These authoring tools target the legacy `core/` content and character runtime
removed by the architecture rebuild in commit `730520a`. They are retained for
historical reference but excluded from Godot resource scanning. Current game
content is built from `game/content/` and the active tools in `tools/`.
